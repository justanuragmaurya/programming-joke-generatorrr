<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Programming Joker</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            color: #333;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        h1 {
            font-size: 2.5em;
            color: #ff6347;
            text-align: center;
            margin-bottom: 20px;
        }

        form {
            text-align: center;
            margin-bottom: 20px;
        }

        input[type="submit"] {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }

        input[type="submit"]:hover {
            background-color: #45a049;
        }

        p {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            margin-left: auto;
            margin-right: auto;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            font-size: 1.2em;
            text-align: center;
            max-width: 600px;
        }

        .container {
            text-align: center;
        }
    </style>   
</head>
<body>
    <div class="container">
        <h1>Programming Joker 🤡</h1>
        <form action="/getjoke" method="post">
            <input type="submit" value="Joke">
        </form>
        <% if(locals.data){%>
        <p><%= data %></p>
        <%}%>
    </div>
</body>
</html>
